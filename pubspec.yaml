name: discover_bulgaria
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  http: ^1.1.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.5.0  # Добави Supabase SDK
  flutter_dotenv: ^5.1.0    # За .env файлове
  uuid: ^4.0.0

  google_maps_flutter: ^2.5.0
  geolocator: ^11.0.0
  url_launcher: ^6.2.5



dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/quiz.webp

